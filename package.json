{
  "name": "Promo Tech",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd service && node server.js",
    "client": "cd client && npm start"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
